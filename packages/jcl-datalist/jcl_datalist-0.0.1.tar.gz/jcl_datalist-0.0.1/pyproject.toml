[build-system]
requires = ["hatchling>=1.5.0", "jupyterlab>=4.0.0,<5", "hatch-nodejs-version"]
build-backend = "hatchling.build"

[project]
name = "jcl_datalist"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"
classifiers = [
    "License :: OSI Approved :: BSD License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Framework :: Jupyter",
]
dependencies = [
    "bokeh >=1.0.0,!=2.0.0",
    "distributed>=1.24.1",
    "jupyter-server-proxy>=1.3.2",
    "jupyterlab>=4.0.0,<5",
]
dynamic = ["version", "description", "authors", "urls", "keywords"]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-cov",
    "pytest-html",
    "black",
    "flake8",
]

[tool.hatch.version]
source = "nodejs"

[tool.hatch.metadata.hooks.nodejs]
fields = ["description", "authors", "urls"]

[tool.hatch.build.targets.sdist]
artifacts = ["jcl_datalist/labextension"]
exclude = [".github"]

[tool.hatch.build.targets.wheel.shared-data]
"jupyter-config" = "etc/jupyter"
"jcl_datalist/labextension" = "share/jupyter/labextensions/jcl-datalist"
"install.json" = "share/jupyter/labextensions/jcl-datalist/install.json"

[tool.hatch.build.hooks.version]
path = "jcl_datalist/_version.py"

[tool.hatch.build.hooks.jupyter-builder]
dependencies = ["hatch-jupyter-builder>=0.5"]
build-function = "hatch_jupyter_builder.npm_builder"
ensured-targets = [
    "jcl_datalist/labextension/static/style.js",
    "jcl_datalist/labextension/package.json",
]
skip-if-exists = ["jcl_datalist/labextension/static/style.js"]

[tool.hatch.build.hooks.jupyter-builder.build-kwargs]
build_cmd = "build:prod"
npm = ["jlpm"]

[tool.hatch.build.hooks.jupyter-builder.editable-build-kwargs]
build_cmd = "build"
npm = ["jlpm"]
source_dir = "src"
build_dir = "jcl_datalist/labextension"

[tool.check-wheel-contents]
ignore = ["W002"]

[tool.black]
# target-version should be all supported versions
target_version = [
    "py38",
    "py39",
    "py310",
    "py311",
]

[tool.pytest.ini_options]
cache_dir = "build/.cache/pytest"
addopts = [
    "-vv",
    "--cov=jcl_datalist",
    "--cov-branch",
    "--cov-context=test",
    "--cov-report=term-missing:skip-covered",
    "--cov-report=html:build/coverage",
    "--html=build/pytest/index.html",
    "--color=yes",
]

[tool.coverage.run]
data_file = "build/.coverage"
concurrency = [
    "multiprocessing",
    "thread"
]

[tool.coverage.html]
show_contexts = true

[tool.coverage.report]
# Regexes for lines to exclude from consideration
exclude_lines = [
    "if self.debug:",
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]
ignore_errors = true
omit = [
    "jcl_datalist/_version.py",
    "tests/*",
    "*/site-packages/*"
]
