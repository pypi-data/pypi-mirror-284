[tool.poetry]
name = "tqbus-sdk"
version = "0.1.7"
description = ""
authors = ["Rigan <it@turnkeyafrica.com>"]
readme = "README.md"
exclude = ["notebooks/**"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "*"
exrex = "*"
pydantic = "2.*"

[tool.poetry.group.dev.dependencies]
black = {extras = ["d"], version = "^23.11.0"}
python-dotenv = "^1.0.0"
isort = "^5.13.2"
jupyter = "^1.0.0"

[tool.isort]
profile = "black"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
