[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "underdogcowboy"
version = "0.1.0"
description = "An exciting cowboy adventure game"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

[project.scripts]
underdogcowboy = "underdogcowboy.main:main"