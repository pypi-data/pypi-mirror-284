[tool:pytest]
testpaths = tests/unit tests/accuracy
markers = 
	basic: basic tests run for every commit
	extended: test run on PRs
	cern_network: tests that require access to afs or the technical network

[egg_info]
tag_build = 
tag_date = 0

