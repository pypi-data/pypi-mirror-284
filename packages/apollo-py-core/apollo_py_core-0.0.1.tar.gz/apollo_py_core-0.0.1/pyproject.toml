[tool.poetry]
name = "apollo-py-core"
version = "0.0.1"
description = ""
authors = ["Danny Rakita <daniel.rakita@yale.edu>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
apollo-file-py = "^0.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
