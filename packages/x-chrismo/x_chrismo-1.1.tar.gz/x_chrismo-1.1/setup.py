from setuptools import setup

setup(name='x_chrismo',
      version='1.1',
      description='Gaussian distributions',
      packages=['x_chrismo'],
      author = 'Ayo Mosaku',
      zip_safe=False)
