from setuptools import setup, find_packages

setup(
    name='mlprg',
    version='0.2.2',
    description='A package for nothing',
    author='petteer',
    packages=find_packages(),
    install_requires=[],
)
