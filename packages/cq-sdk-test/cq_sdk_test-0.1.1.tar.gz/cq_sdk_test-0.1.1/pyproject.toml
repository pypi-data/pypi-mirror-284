[tool.poetry]
name = "cq-sdk-test"
version = "0.1.1"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.extras]
all = [
    "fire",
]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
cloud = "cq_sdk_test.my_SDK:_fire_entrypoint"