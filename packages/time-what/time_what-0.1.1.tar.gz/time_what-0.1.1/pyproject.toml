[tool.poetry]
name = "time-what"
version = "0.1.1"
description = ""
authors = ["Marc"]
readme = "README.md"
packages = [{include = "time_what", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
