from setuptools import setup, find_packages

setup(
    name="sp_DiffJsonP",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'json'
    ],
    entry_points={
        'console_scripts': [
           
        ],
    },
)
