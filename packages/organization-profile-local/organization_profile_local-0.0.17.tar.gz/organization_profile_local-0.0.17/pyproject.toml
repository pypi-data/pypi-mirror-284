[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "organization-profile-local"
version = "0.0.10" # https://pypi.org/project/organization-profile-local
description = "organization-profile-local Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]
