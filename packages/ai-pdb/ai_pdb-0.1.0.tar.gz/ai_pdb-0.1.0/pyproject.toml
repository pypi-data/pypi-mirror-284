[tool.poetry]
name = "ai-pdb"
version = "0.1.0"
description = "The Python Debugger (PDb) augmented with AI."
authors = ["Daniel Infante Vargas <daninfpj@gmail.com>"]
readme = "README.md"
packages = [{ include = "ai_pdb" }]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
